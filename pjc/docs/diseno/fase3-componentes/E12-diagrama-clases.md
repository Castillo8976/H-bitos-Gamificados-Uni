## Diagrama de Clases

Las 13 clases de entidad del modelo vigente (incluye `NivelCuenta` y `Notificacion`), con visibilidad `-` en atributos y `+` en métodos, y las relaciones (1:1, 1:N, N:M, y la relación derivada `Cuenta → NivelCuenta` sin FK física).

**Archivo editable (Draw.io):** `E12-diagrama-clases.drawio` (misma carpeta) — fuente gráfica vigente, referenciada también desde `M9_clases_metodos.md`.

**Versión histórica (pre-corrección de agosto 2026, incluye la clase `Reporte` ya eliminada — se conserva solo como referencia):**

![Diagrama de clases histórico](../imagenes/DiagramaClases.png)

> Ver `M9_clases_metodos.md` para la trazabilidad método ↔ CU ↔ RF de cada clase, y `M10_diagramas_secuencia.md` para la clasificación de los participantes de secuencia frente a estas clases.
